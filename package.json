{
  "name": "ca-creators",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "next build",
    "dev": "cross-env NODE_ENV=dev next dev",
    "export": "npm run build && next export && touch out/.nojekyll && touch out/CNAME && echo cacreators.com >> out/CNAME",
    "start": "next start",
    "deploy": "npm run export && git add . && git commit --amend --no-edit && git push && git subtree push --prefix=out origin gh-pages"
  },
  "dependencies": {
    "isomorphic-unfetch": "^3.0.0",
    "moment": "^2.24.0",
    "next": "9.3.0",
    "react": "16.13.0",
    "react-dom": "16.13.0",
    "webp-loader": "^0.6.0"
  },
  "devDependencies": {
    "@types/node": "^13.9.1",
    "@types/react": "^16.9.23",
    "cross-env": "^7.0.2",
    "husky": "^4.2.3",
    "typescript": "^3.8.3"
  }
}
