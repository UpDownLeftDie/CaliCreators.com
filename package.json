{
  "name": "twitch-socal",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "next build",
    "dev": "next dev",
    "export": "npm run build && next export",
    "start": "next start"
  },
  "dependencies": {
    "next": "9.0.5",
    "react": "16.9.0",
    "react-dom": "16.9.0"
  },
  "devDependencies": {
    "@types/node": "^12.7.2",
    "@types/react": "^16.9.2",
    "husky": "^3.0.4",
    "typescript": "^3.6.2"
  },
  "husky": {
    "hooks": {
      "pre-commit": "touch out/.nojekyll && touch out/CNAME && echo \"twitchsocal.com\" >> out/CNAME && git add ."
    }
  }
}
